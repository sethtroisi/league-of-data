Convert to one minute blocks (to match frame size)

Convert away from DnnClassifier (maybe to canned.DnnClassifier)

Clean up prediction / eval code (eval_input_fn?)

Test champ features on ARAM games
Work on champ embedding for normal model

limit GPU memory to 80%
