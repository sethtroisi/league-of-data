Reach 80% accuracy and checkpoint this repo

Rename files to lower case (and verify PyCharm is happy)

Convert away from DnnClassifier

Test champ features on ARAM games
Work on champ embedding for normal model
